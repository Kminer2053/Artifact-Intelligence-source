{
  "user": "default",
  "_doc": "기본 프로파일(사장님). 아직 개인화 판정 항목 없음 — 원장에서 범위='개인' 판정이 나오면 여기 등재된다. overrides는 1층 값 덮어쓰기(근거 필수), 성향은 재량 기본값.",
  "overrides": [],
  "성향": {
    "byline_기본부서": "AI혁신처"
  }
}
