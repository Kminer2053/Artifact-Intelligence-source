# 정책 토큰 — 보통은 설치 시 자동 발급됩니다(부트스트랩이 정책서버토큰.conf 를 만듭니다).
# 이 예시는 수동으로 지정할 때만 씁니다:
#   ① 이 파일을 '정책서버토큰.conf' 로 복사한 뒤 아래 마지막 주석 자리에 토큰 한 줄만 남긴다
#   ② 또는 환경변수로:  export 문서지능_정책토큰=<토큰>   (이게 우선)
# 문의: park2053@gmail.com
# <여기에-토큰-한-줄>
